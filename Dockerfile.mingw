FROM debian:bookworm-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc-mingw-w64-i686 \
    g++-mingw-w64-i686 \
    make \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /src

# Default: build the DLL
CMD ["make", "-f", "Makefile.mingw", "CC=i686-w64-mingw32-g++"]
